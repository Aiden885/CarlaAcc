

### ACC自适应巡航系统决策逻辑详解

#### 第一部分：系统输入 - 驾驶指令 

系统能够识别7种不同的驾驶指令 (`I_m`)，这些指令源于驾驶员的操作，并遵循不同的控制原则：

| 符号 | 指令名称 | 驾驶操作 | 驾驶意图 | 原则 |
| :--- | :--- | :--- | :--- | :--- |
| **`I_0`** | 降速 | ACC 降速键被按 | ACC 降速或当速启控 | 机驾执行 |
| **`I_1`** | 增速 | ACC 增速键被按 | ACC 增速或继承启控 | 机驾执行 |
| **`I_2`** | 降距 | ACC 降距键被按 | ACC 降距 | 机驾执行 |
| **`I_3`** | 增距 | ACC 增距键被按 | ACC 增距 | 机驾执行 |
| **`I_4`** | 油门 | 油门踏板被踩下 | 人工加速 | 人驾优先 |
| **`I_5`** | 刹车 | 制动踏板被踩下 | 人工减速 | 人驾优先 |
| **`I_6`** | 取消 | ACC 取消键被按 | ACC 功能取消 | 无 |

- **机驾执行**：指令由系统直接执行。
- **人驾优先**：驾驶员的操作会覆盖系统的当前控制。

---

#### 第二部分：系统内部状态 

系统在运行时会处于以下4种核心状态 (`S_n`) 之一：

| 符号 | 状态名称 | 含义 |
| :--- | :--- | :--- |
| **`S_0`** | 在控 | 车速为适速，ACC系统正在主动控制车辆。 |
| **`S_1`** | 适速有史待命 | 车速为适速，ACC处于待命状态，且存有上次设定的历史速度数据。 |
| **`S_2`** | 适速无史待命 | 车速为适速，ACC处于待命状态，但没有历史速度数据。 |
| **`S_3`** | 低速 | 车辆处于低速，ACC处于待命状态。 |

---

#### 第三部分：系统输出 - 决策与动作 

根据输入指令和当前状态，系统会执行8种决策 (`R_k`) 中的一种，每种决策都包含具体的操作事项：

| 符号 | 决策名称 | 目标车速 | 控制状态 | 反馈整车 |
| :--- | :--- | :--- | :--- | :--- |
| **`R_1`** | 速度降低 | 速度减 | 控制继续 | 目标车速 |
| **`R_2`** | 速度增加 | 速度增 | 控制继续 | 目标车速 |
| **`R_3`** | 时距降低 | 目标距离减 | 控制继续 | 目标距离 |
| **`R_4`** | 时距增加 | 目标距离增 | 控制继续 | 目标距离 |
| **`R_5`** | 无继控制 | 当前车速 | **进入控制** | 目标车速/距离 |
| **`R_6`** | 继承控制 | 上次目标 | **进入控制** | 目标车速/距离 |
| **`R_7`** | 扭矩仲裁 | 无关 | 控制继续 | 目标车速/距离 |
| **`R_8`** | 系统待命 | 无关 | **进入待命** | ACC 待命 |

---

#### 第四部分：核心逻辑 - 系统状态转移指令 

这套系统的核心逻辑，即如何根据【指令 `I_m`】和【当前状态 `S_n`】决定执行哪个【决策 `R_k`】，并转移到新的状态，具体如下：

**1. 当前状态：`S_0` 在控**
* **指令 `I_0`/`I_1` (降速/增速)** → **决策 `R_1`/`R_2` (速度降低/增加)**，状态**保持 `S_0` 在控**。
* **指令 `I_2`/`I_3` (降距/增距)** → **决策 `R_3`/`R_4` (时距降低/增加)**，状态**保持 `S_0` 在控**。
* **指令 `I_4` (油门)** → **决策 `R_7` (扭矩仲裁)**，状态**保持 `S_0` 在控**（但控制权可能被驾驶员短暂超越）。
* **指令 `I_5`/`I_6` (刹车/取消)** → **决策 `R_8` (系统待命)**，**新状态**变为** `S_1` 适速有史待命**（因为退出时保存了历史数据）。

**2. 当前状态：`S_1` 适速有史待命**
* **指令 `I_1` (增速)** → **决策 `R_6` (继承控制)**，**新状态**变为** `S_0` 在控**。
* **指令 `I_0` (降速)** → **决策 `R_5` (无继控制)**，**新状态**变为** `S_0` 在控**。
* **指令 `I_2` 至 `I_6` (其他)** → **决策 `R_8` (系统待命)**，状态**保持 `S_1` 待命**。

**3. 当前状态：`S_2` 适速无史待命**
* **指令 `I_0` (降速)** → **决策 `R_5` (无继控制)**，**新状态**变为** `S_0` 在控**。
* **指令 `I_1` 至 `I_6` (其他)** → **决策 `R_8` (系统待命)**，状态**保持 `S_2` 待命**。

**4. 当前状态：`S_3` 低速** 如果车速小于V_min_kmh或min_speed_kmh就进入低速状态，大于最低车速要求就会自动进入`S_1`或者`S_2`取决于有没有历史数据
* **任何指令 (`I_0` 至 `I_6`)** → **决策 `R_8` (系统待命)**，状态**保持`S_3`待命**。