# 基于 CARLA 仿真平台的自适应巡航控制(ACC)系统 —— 年度项目汇报

> **项目周期**: 2025年5月8日 — 2026年1月29日 (约9个月)
> **开发者**: ZhaoY
> **技术栈**: Python + MATLAB/Simulink + CARLA 仿真器
> **版本管理**: Git (111次提交, 12个分支)

---

## 一、项目概述

本项目基于 CARLA 自动驾驶仿真平台，设计并实现了一套**自适应巡航控制(ACC)系统**。系统采用 **Python-Simulink 联合仿真架构**，Python 端负责 CARLA 仿真环境交互、感知处理和人机界面，Simulink 端负责 **决策状态机** 和 **SPPVT(Set Point with Preview and Transition)控制算法** 的运算。双端通过 **UDP 实时通信**，实现了完整的"感知-决策-控制-执行"闭环。

项目经历了从早期原型探索、算法迭代、架构重构到最终集成测试的完整工程周期，最终形成了一个功能完善、架构清晰的 ACC 系统。

---

## 二、项目整体数据统计

| 指标 | 数值 |
|------|------|
| 项目总周期 | 约 267 天 |
| Git 提交总数 | **111** 次 |
| 分支总数 | **12** 个 (含远程分支) |
| 最终 Python 源文件数 | **44** 个 |
| 最终 MATLAB/Simulink 文件数 | **12** 个 (.m + .slx) |
| Python 总代码行数 (最终) | **约 12,800 行** |
| 提交最密集月份 | 10月 (24次), 5月 (23次) |

### 月度提交分布

```
2025-05  ████████████████████████  23次  — 项目启动 & 原型开发
2025-06  █████                      5次  — LKA稳定 & Simulink通信
2025-07  █                          1次  — 扭矩仲裁
2025-09  ████████████████          16次  — 架构重构 & Simulink集成
2025-10  ████████████████████████  24次  — 性能优化 & 实时可视化
2025-11  ███████████████████       19次  — 场景实现 & 架构大改
2025-12  █████████████████         17次  — UDP集成 & 算法精调
2026-01  ██████                     6次  — 增量控制 & 约束限制
```

---

## 三、项目演进历程 (按时间线)

### 第一阶段: 原型探索与 MPC 控制 (2025.05.08 — 2025.05.16)

**核心目标**: 搭建基础仿真框架，实现感知与控制原型

**主要工作**:

1. **项目初始化 (5.08)**: 创建项目骨架，实现了多种 ACC 控制器变体 (`acc_obj.py`, `acc_points.py`, `acc_pub.py`, `acc_ros.py`)，同时搭建了感知模块(Kalman滤波器、车道线检测、雷达点云聚类)。此时感知与控制**尚未连通**。

2. **MPC 纵向控制 (5.11)**: 引入 **模型预测控制(MPC)** 算法实现纵向(速度/距离)控制。创建 `acc_planning_control.py` 封装 MPC 优化器，`direct_perception_control.py` 实现直接感知-控制管线。

3. **MPC 横向控制 (5.11-5.12)**: 将 MPC 扩展到横向(转向)控制。经过一晚的调试，成功实现了横纵向联合 MPC 控制。前车速度较快时仍有轻微偏移。

4. **场景丰富化 (5.13-5.16)**:
   - 实现了**无前车自主跟随**模式
   - 加入**车道线检测**并大幅重写检测算法
   - 实现了**前车周期性变速**(正弦波速度模式)
   - 在 CARLA **Town6 地图**上实现车道保持

**创建的核心模块**: `acc_planning_control.py`(MPC控制), `direct_perception_control.py`(直接感知), `lane_detection.py`(车道检测), `kalman_filter.py`(状态估计), `radar_cluster.py`(雷达聚类), `sinusoidal_speed_controller.py`(前车正弦速度控制)

---

### 第二阶段: LKA 车道保持与 SPPVT 算法引入 (2025.05.18 — 2025.06.03)

**核心目标**: 实现车道保持辅助(LKA)，引入 SPPVT 控制算法和多模式状态机

**主要工作**:

1. **LKA 开发 (5.18-5.21)**:
   - 独立正弦前车轨迹模块
   - 实现 3D 轨迹可视化 (`carla_3d_trajectory_visualizer.py`) 和实时轨迹管理
   - 基于 **PID** 实现车道线跟踪，替代 MPC 横向控制（更简单稳定）
   - 完成测试报告运行版本

2. **代码精简 (5.26)**: **大规模清理**——删除了 5 个实验性模块 (`acc_obj.py`, `acc_points.py`, `acc_pub.py`, `acc_ros.py`, 3D轨迹可视化模块)，共减少 ~3,800 行代码，集中在 cruise 模式开发。

3. **SPPVT 算法实现 (5.27)**: **关键里程碑**——实现 SPPVT(Set Point with Preview and Transition) 纵向控制算法 (`sppvt_longitudinal_control.py`, 322行)。在此基础上引入**三模式控制器** (`three_mode_controller.py`, 293行)，提供巡航(cruise)、跟车(follow)、制动(brake)三种行为模式。

4. **状态机与人机交互 (5.28)**:
   - 创建 ACC 决策模块 `acc_decision.py` (645行)，实现 S0(在控)/S1(有史待命)/S2(无史待命) 状态转换
   - 创建键盘交互模块 `acc_manual_control.py` (686行)，支持 E/Q/S 键控制
   - 创建整合主循环 `acc_updated.py` (583行)

5. **稳定与发布 (5.30-6.03)**:
   - 添加 HUD 显示管理 (`display_manager.py`, 390行)
   - 修复巡航模式下的碰撞 bug
   - 完善三模式控制器边界条件
   - 6月3日声明系统**"完全实现"**——第一个完整的 Python 版 ACC 系统

**架构成果**: Python 端完整的 ACC 系统：感知 → 决策(状态机) → 控制(SPPVT+三模式) → 执行 → HUD显示

---

### 第三阶段: Simulink 联合仿真起步 (2025.06.20 — 2025.07.17)

**核心目标**: 将 SPPVT 控制计算从 Python 迁移到 Simulink，建立联合仿真架构

**主要工作**:

1. **Simulink SPPVT 模型 (6.20)**: **范式转变**——创建 `sppvt_control_model.slx` (Simulink 模型)，将 SPPVT 计算从纯 Python 迁移到 MATLAB/Simulink。通过 `matlab_connect.py` 实现 Python-MATLAB 通信桥接。此后项目正式进入 **CARLA-Simulink 联合仿真**模式。

2. **扭矩仲裁 (7.17)**: 实现扭矩仲裁机制——解决 ACC 控制、制动、手动覆盖之间的冲突。同时清理了整个 `ran/` 历史代码归档目录（删除 ~1,800 行遗留代码）。

---

### 第四阶段: 架构重构与 Simulink 集成深化 (2025.09.05 — 2025.09.30)

**核心目标**: 简化控制架构，建立完整的 Python-Simulink 桥接

**主要工作**:

1. **架构简化 (9.05-9.09)**:
   - 在 `master` 分支上尝试 Stateflow 决策实现（使用 MATLAB 脚本自动生成）
   - **三模式→二模式**: 删除三模式控制器(运行时负荷过大)，切换为**二模式控制**架构 (`two_mode_controller.py`)

2. **Python 决策层 (9.10-9.11)**:
   - 全新 Python 决策逻辑实现并通过测试
   - 扭矩仲裁功能完成，pygame HUD 显示调试成功
   - 创建 `pygame_display.py`, `sensor_manager.py` 等新模块

3. **Simulink 统一模块 (9.23)**: **本阶段最大提交** (+9,633行)——创建集成 Simulink 模型 `ACC_Decision_SPPVT_Integrated.slx` (183KB)。配套创建:
   - `build_acc_decision_sppvt_model.m` — 模型构建脚本
   - `create_decision_sppvt_bus.m` — 总线定义
   - `decision_function.m` — 决策函数
   - `sppvt_function.m` — SPPVT 计算函数
   - `acc_decision_sppvt_interface.py` — Python-Simulink 接口

4. **CARLA-Simulink 桥接 (9.24-9.26)**:
   - 创建 `realtime_sppvt_state_manager.py` (651行) 实现实时状态管理
   - 确认能够成功从 Simulink 获取输出数据
   - 完成 CARLA-Simulink 数据对齐

5. **总线扩展与 Bug 修复 (9.29-9.30)**:
   - 扩展 Simulink 总线，新增三个状态字段
   - 修复自车和前车都不移动的 bug
   - 解决前车移动过慢的问题（世界频率/计算速度相关）

---

### 第五阶段: 性能优化与可视化 (2025.10.10 — 2025.10.30)

**核心目标**: 优化实时性能，完善输出体系，增强可视化

**主要工作**:

1. **性能达标 (10.10)**: 控制循环周期优化到 **~100ms**，满足实时性需求。

2. **输出体系完善 (10.13-10.16)**:
   - 调整输出数据结构，全面输出 **18 个字段**
   - 实现期望时距与实际时距的绘图和 CSV 日志
   - 代码模块化——从 `acc_updated.py` 提取出:
     - `output_formatter.py` (187行) — 输出格式化
     - `sensor_transforms.py` (80行) — 传感器坐标变换
     - `vehicle_utils.py` (118行) — 车辆工具函数

3. **感知方案切换 (10.17)**: 将前车和车道线检测从**视觉感知**切换为**CARLA API 直接获取**。创建 `carla_perception.py` (191行) 和 `lateral_pid_controller.py` (164行)。发现速度超过 100km/h 时存在震荡。

4. **HUD 与交互 (10.20-10.22)**:
   - 重新设计 HUD 显示系统
   - 会后重构，支持 18 字段输出
   - 创建 `simulink_call_manager.py` (330行)

5. **总线数组与跨平台 (10.23-10.27)**:
   - 成功完成 Simulink **总线数组扩展**，升级功能正常运行
   - 完成 **Ubuntu/Linux 移植**，解决 MATLAB Engine 兼容性问题

6. **实时可视化 (10.29-10.30)**:
   - 创建 `realtime_time_gap_plotter.py` (352行) 实现实时时距绘图
   - 在仿真运行过程中实时展示跟车性能曲线

---

### 第六阶段: 场景拓展与架构大改 (2025.11.03 — 2025.12.08)

**核心目标**: 丰富测试场景，实现 Facade 架构模式，完成 Decision+SPPVT 的 UDP 集成

**主要工作**:

1. **扭矩-油门映射 (11.03-11.07)**:
   - 研究车辆物理参数，创建扭矩-油门转换器
   - PID 加速度-油门映射、扭矩曲线标定、查表法等多种方案
   - 最终实现两种巡航模式：百分比模式 和 **Constant Velocity 模式**
   - 大规模清理实验性脚本 (-10,525行)

2. **新功能开发 (11.11)**:
   - 前车**斜坡速度**功能 (`ramp_speed_controller.py`)
   - **增强横向控制器** (`enhanced_lateral_controller.py`) 和**换道控制器** (`lane_change_controller.py`)

3. **测试框架建立 (11.17)**:
   - 提取 ACC 配置 → `acc_config.py` (127行)
   - 创建结果绘图器 → `result_plotter.py` (482行)，支持 CSV+PNG 自动保存

4. **Simulink 架构重构 (11.18-11.21)**: **架构转折点**
   - 11.19: 首次成功跑通修改后的 Simulink 集成，创建控制器层级(acc_controller, hybrid_controller, sppvt_manager)
   - 11.20: 引入 **Facade 模式** (`acc_control_facade.py`)，大幅简化架构，一次性删除 6+ 模块 (~3,000+ 行)
   - 11.21: 定义 JSON 驱动的**决策状态机** (`state_transitions.json`)，以查表方式实现 S0/S1/S2/S3 状态转换

5. **测试场景实现 (11.24-11.28)**:
   - **切入场景** (`cut_in_scenario.py`, 211行) — 邻车切入本车道
   - **切出场景** (`cut_out_scenario.py`, 126行) — 前车切出本车道
   - **制动减速度**逻辑实现，基于扭矩控制
   - 采集弯道、上下坡、切入切出等多场景测试数据

6. **决策 Simulink 模型 (12.01-12.03)**:
   - SPPVT 参数分类完成
   - 创建独立的**决策 Simulink 模型** `acc_decision_core.slx`，基于查表实现状态转换
   - 生成 Rapid Accelerator 编译产物以加速执行

7. **UDP 集成里程碑 (12.05-12.08)**: **最关键的架构决策**
   - 12.05: 尝试使用**分开的 UDP 通道**分别连接 Decision 和 SPPVT 模型 → **失败**
   - 12.08: **将 Decision 和 SPPVT 合并为单一 Simulink 模型** `acc_integrated_model.slx`，通过**单 UDP 通道** (端口 27000/27001) 通信。创建 `integrated_simulink_manager.py` (523行) 和 `start_integrated_simulink_server.m` (83行)。**此架构一直沿用至项目结束。**

---

### 第七阶段: 算法精调与稳定性优化 (2025.12.09 — 2025.12.31)

**核心目标**: 解决工程问题，优化控制算法，准备汇报演示

**主要工作**:

1. **主程序分解 (12.09)**: 将单体 `acc_updated.py` 分解为多个职责模块:
   - `carla_system_initializer.py` (464行) — 系统初始化
   - `control_loop_manager.py` (543行) — 控制循环管理
   - `input_manager.py` (194行) — 输入处理
   - `resource_manager.py` (219行) — 资源管理
   - `system_state.py` (145行) — 系统状态

2. **内存泄漏修复 (12.11)**: 修复 matplotlib/pygame 渲染导致的内存泄漏问题。

3. **SPPVT 算法精调 (12.15-12.18)**:
   - 添加**升级冷却参数**和修改 rho 参数，防止频繁升级
   - 实现升级突变时**控制输出清零**，防止车辆行为突变
   - 在 Simulink 中添加**低通滤波器**，平滑控制输出

4. **汇报准备 (12.22)**: 添加**传感器噪声模拟器** (`sensor_noise_simulator.py`) 用于更真实的测试。调优参数用于汇报演示。

5. **Simulink 条件与计时 (12.23-12.31)**: 在 Simulink 中添加**条件逻辑**和**计时器机制**，实现基于双条件 + 冷却时间的状态转换。采集多组测试数据。

---

### 第八阶段: 增量控制与约束限制 (2026.01.12 — 2026.01.29)

**核心目标**: 实现增量控制算法，添加安全约束，大规模验证测试

**主要工作**:

1. **升级条件修改 (1.12)**: 修改升级条件逻辑，制动方式从油门控制切换为**扭矩控制**，增强第5个实时图表。记录 Simulink 架构文档。

2. **增量控制 (1.14)**: **控制算法根本性变革**——从绝对控制切换到**增量控制**:
   ```
   Y(k) = Y(k-1) + ΔY    (增量形式)
   ```
   替代原来的：
   ```
   Y(k) = Kp * (error + stage_offset) + Y0    (绝对形式)
   ```
   创建 `integrated_simulink_manager.py` 中的增强误差计算逻辑。

3. **加速度约束 (1.27)**: 添加**纵向加速度/减速度约束** (`longitudinal_constraint_limiter.py`, 145行)，确保乘客舒适性和车辆安全。同时引入 LKS (Lane Keeping System) Simulink 模型。

4. **大规模验证 (1.28)**: 进行了 **12 组完整测试**，系统保存 CSV+PNG 测试结果。

5. **会后存档 (1.29)**: 根据会议反馈进行参数调优，更新 Simulink 模型和车辆物理参数。

---

## 四、关键技术难点与解决方案

### 4.1 UDP 响应错位导致的状态振荡 (2026.02.05 发现并修复)

**问题**: ACC 控制系统在"控制(S0)"和"待命(S2)"状态之间频繁切换，HUD 闪烁。

**根因**: `SimulinkUDPClient` 的 `send_initial_packet=True` 导致 UDP 响应错位一帧——初始包的响应未被消费，后续每次 `call()` 接收到的都是上一帧的响应。

**解决方案**: 设置 `send_initial_packet=False`。

### 4.2 双 UDP 通信方案失败 (2025.12.05)

**问题**: 尝试为 Decision 和 SPPVT 分别使用独立 UDP 通道，通信无法正常工作。

**解决方案**: 将两个 Simulink 子系统合并为单一模型 `acc_integrated_model.slx`，通过单 UDP 通道通信。

### 4.3 内存泄漏 (2025.12.11)

**问题**: 长时间运行后内存持续增长，最终影响性能。

**解决方案**: 修复 matplotlib/pygame 渲染资源未正确释放的问题，添加实时绘图的最大点数限制。

### 4.4 SPPVT 阶段升级突变 (2025.12.15-12.16)

**问题**: 控制阶段(stage)升级时控制输出突变，导致车辆行为不平滑。

**解决方案**: (1) 添加升级冷却参数防止频繁升级; (2) 升级时控制输出清零; (3) 添加低通滤波器平滑输出。

### 4.5 高速震荡 (2025.10.17)

**问题**: 速度超过 100km/h 时出现控制震荡。

**解决方案**: 将感知方案从视觉检测切换为 CARLA API 直接获取，减少感知延迟和噪声。

---

## 五、系统最终架构

### 5.1 整体架构

```
┌─────────────────────────────────────────────────────┐
│                    CARLA 仿真环境                      │
│  ┌───────────┐  ┌──────────┐  ┌───────────────────┐ │
│  │  自车(Ego) │  │ 前车(Lead)│  │  场景(Town/Map)    │ │
│  └───────────┘  └──────────┘  └───────────────────┘ │
└──────────┬──────────────────────────┬───────────────┘
           │ 状态数据                    │ 控制指令
           ▼                           ▲
┌──────────────────────────────────────────────────────┐
│                  Python 端                            │
│                                                      │
│  ┌─────────────────┐    ┌──────────────────────────┐│
│  │ carla_perception │    │ control_loop_manager     ││
│  │ (CARLA API感知)  │    │ (控制循环管理)            ││
│  └────────┬────────┘    └────────┬─────────────────┘│
│           │                      │                   │
│           ▼                      ▼                   │
│  ┌─────────────────┐    ┌──────────────────────────┐│
│  │  acc_controller  │    │integrated_simulink_mgr   ││
│  │  (ACC控制器)     │◄──►│ (Simulink管理器)         ││
│  └─────────────────┘    └────────┬─────────────────┘│
│                                  │ UDP (27000/27001)  │
│  ┌─────────────────┐            │                    │
│  │ display_manager  │            │                    │
│  │ (HUD显示)       │            │                    │
│  └─────────────────┘            │                    │
└──────────────────────────────────┼────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────┐
│              MATLAB/Simulink 端                       │
│          acc_integrated_model.slx                     │
│                                                      │
│  ┌──────────────────┐    ┌─────────────────────────┐│
│  │ Decision 子系统    │    │   SPPVT 子系统           ││
│  │ (状态机查表决策)   │───►│ (控制方程+阶段管理)      ││
│  └──────────────────┘    └─────────────────────────┘│
│                                                      │
│  控制方程: Y(k) = Y(k-1) + Kp * Δ(error+offset)     │
└──────────────────────────────────────────────────────┘
```

### 5.2 Python 端核心模块 (最终版本)

| 模块 | 行数 | 职责 |
|------|------|------|
| `acc_updated.py` | 640 | 主程序入口 |
| `control_loop_manager.py` | 638 | 控制循环管理 |
| `integrated_simulink_manager.py` | 622 | Simulink UDP 通信管理 |
| `result_plotter.py` | 614 | 测试结果绘图 (CSV+PNG) |
| `realtime_time_gap_plotter.py` | 536 | 实时时距绘图 |
| `display_manager.py` | 483 | HUD 显示管理 |
| `carla_system_initializer.py` | 477 | CARLA 系统初始化 |
| `two_mode_controller.py` | 429 | 二模式控制器 |
| `output_formatter.py` | 347 | 输出格式化 |
| `torque_to_throttle_converter.py` | 307 | 扭矩-油门转换 |
| `acc_controller.py` | 288 | ACC 控制器核心 |
| `simulink_udp_interface.py` | 235 | UDP 底层通信 |
| `carla_perception.py` | 234 | CARLA API 感知 |
| `acc_config.py` | 205 | 集中式配置 |
| `cut_in_scenario.py` | 211 | 切入测试场景 |
| `cut_out_scenario.py` | 126 | 切出测试场景 |
| `longitudinal_constraint_limiter.py` | 145 | 加速度约束 |
| `sensor_noise_simulator.py` | 211 | 传感器噪声模拟 |

### 5.3 Simulink 端核心模型

| 模型/脚本 | 职责 |
|-----------|------|
| `acc_integrated_model.slx` (149KB) | **最终统一模型**: Decision + SPPVT |
| `start_integrated_simulink_server.m` | UDP 服务器启动脚本 |
| `create_decision_sppvt_bus.m` | 总线定义 |
| `decision_lookup_data.mat` | 决策查表数据 |
| `stage_manager.m` | 阶段管理器 |

### 5.4 状态机设计

```
       Cmd1(E键)            Cmd6(S键)
S2 ──────────────► S0 ──────────────► S1
(无史待命)    (设置has_history)  (在控)              (有史待命)
     ▲                        │  ▲                    │
     │                  Cmd0  │  │  Cmd1(E键)         │
     │                 (保持)  ▼  │                    │
     │                   S0 ──┘                       │
     │                                                │
     └────────────────────────────────────────────────┘
                     超时/条件触发
```

---

## 六、分支策略与开发流程

| 分支 | 用途 | 活跃期 |
|------|------|--------|
| `master` | 主干，二模式基础版本 | 至 9.09 |
| `mpc_justx` | MPC 横向控制实验 | 5.11 |
| `mpc_no_tune` | MPC 未调参版本 + 上下坡 | 5.15 |
| `period516` | 前车周期性变速实验 | 5.16 |
| `follow_with_traj` | 轨迹跟随 + LKA + Simulink通信 | 5.19 — 7.17 |
| `two_mode` | 二模式控制 + 总线数组 | 10.10 — 10.24 |
| `two_mode_updates` | 同步异步尝试 | 10.21 |
| **`after_meeting1022`** | **主开发分支** (10.22会后至今) | 10.22 — 1.29 |
| `codex/update-variable-name-in-multiple-files` | 自动变量名重命名 | 6.06 |

> **开发模式**: 项目采用"分支实验，主线推进"的模式。早期在多个分支上探索不同技术路线 (MPC/LKA/轨迹跟随)，10月22日会后在 `after_meeting1022` 分支上进行长期持续开发，成为事实上的主开发分支。

---

## 七、项目里程碑总览

| 日期 | 里程碑 | 意义 |
|------|--------|------|
| 2025.05.08 | 项目启动 | 搭建基础框架 |
| 2025.05.12 | MPC 纵横向控制实现 | 首个完整控制算法 |
| 2025.05.27 | SPPVT 算法 + 三模式控制器 | 核心控制算法确立 |
| 2025.06.03 | Python 版 ACC "完全实现" | 第一个完整系统 |
| 2025.06.20 | Simulink 联合仿真建立 | 架构范式转变 |
| 2025.09.09 | 三模式→二模式简化 | 架构精简 |
| 2025.09.23 | Simulink 集成模块完成 | Python-Simulink 桥接 |
| 2025.10.10 | 控制周期达到 100ms | 实时性达标 |
| 2025.10.17 | 感知切换为 CARLA API | 感知方案定型 |
| 2025.11.20 | Facade 架构模式引入 | 架构规范化 |
| 2025.11.25 | 切入/切出场景实现 | 场景覆盖增强 |
| 2025.12.08 | **单 UDP 集成模型** | 最终通信架构确立 |
| 2025.12.18 | Simulink 低通滤波器 | 控制平滑性提升 |
| 2026.01.14 | **增量控制**算法 | 控制精度提升 |
| 2026.01.27 | 加速度约束 + LKS 模型 | 安全性增强 |

---

## 八、技术能力成长与总结

### 8.1 技术栈覆盖

- **仿真平台**: CARLA (Python API, 多地图, 多场景)
- **控制算法**: MPC → SPPVT → 增量控制，PID 横向控制
- **联合仿真**: Python-MATLAB/Simulink 通信 (MATLAB Engine → UDP)
- **Simulink 建模**: 状态机、查表决策、总线定义、S-Function、低通滤波器
- **软件工程**: Facade 模式、模块分解、JSON 配置驱动、实时可视化
- **测试验证**: 多场景(弯道/上下坡/切入切出)自动化测试 + 结果存档

### 8.2 工程实践亮点

1. **架构演进**: 从单体 Python 脚本 → 模块化 Python + 独立 Simulink → 统一 UDP 集成架构，经历了3次大的架构重构
2. **技术选型迭代**: MPC→SPPVT, 三模式→二模式, 视觉感知→API感知, MATLAB Engine→UDP, 绝对控制→增量控制——每次技术选型变更都基于实际问题驱动
3. **代码质量管理**: 多次大规模清理(5.26 删除实验代码, 9.09 删除三模式, 11.10 清理标定脚本, 11.20 Facade重构)，保持代码库精简
4. **问题诊断能力**: UDP 一帧错位、内存泄漏、阶段升级突变等问题的定位和修复展现了系统性的调试思路

### 8.3 量化成果

- 从零构建了一个 **~12,800 行 Python + Simulink 模型** 的完整 ACC 系统
- 完成 **111 次版本提交**，平均每 2.4 天一次提交
- 覆盖 **4 类测试场景** (弯道跟车、上下坡、切入、切出)
- 系统控制周期 **~100ms**，满足实时性需求
- 保存了 **20+ 组** 完整测试结果 (CSV+PNG)

---

*文档生成日期: 2026年2月6日*
*基于 Git 仓库 111 次提交的完整历史分析*
