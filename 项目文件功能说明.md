# ACC 项目文件功能说明

从主入口 `acc_updated.py` 出发的完整文件功能讲解

---

## 📋 整体架构图

```
acc_updated.py (主入口)
    ↓
    ├─ 1. ACCConfig (配置加载)
    ├─ 2. ResourceManager (资源管理)
    ├─ 3. DisplayManager (显示界面)
    ├─ 4. CarlaSystemInitializer (CARLA初始化)
    ├─ 5. ACCControlFacade (ACC控制器)
    ├─ 6. InputManager (输入处理)
    ├─ 7. ControlLoopManager (控制循环)
    ├─ 8. CSV记录
    └─ 9. Plotter (绘图器)
```

---

## 📦 第1步：配置系统

### **acc_config.py** - 配置中心
**职责**：集中管理所有系统参数

```python
# 188行 - 配置参数分类：
- CARLA连接配置：host, port, map_name, fixed_delta_seconds
- 车辆生成配置：ego_vehicle_blueprint, spawn_location
- ACC参数：V_target_kmh=50, G2_s=4.0, V_min_kmh=30
- SPPVT参数：rho=0.2, max_accel=2.0, upgrade_cooldown=2
- 扭矩转换：sppvt_accel_scale=210, sppvt_decel_scale=1.0
- UDP通信：integrated_udp_send_port=27000/27001
- 场景配置：enable_cut_in_scenario, cut_in_trigger_time_s
- 绘图器配置：use_result_plotter (统一配置入口)
```

**关键方法**：
- `get_acc_params()`: 获取 ACC 参数副本
- `__str__()`: 打印配置摘要

---

## 🔧 第2步：资源管理

### **resource_manager.py** - 资源生命周期管理器
**职责**：统一管理所有 CARLA 资源，确保程序退出时正确清理

```python
# resource_manager.py - 120行
核心功能：
1. register_carla_actor()：注册车辆、传感器
2. register_cleanable()：注册需要清理的对象（实现Cleanable接口）
3. register_file()：注册文件句柄
4. cleanup_all()：程序退出时统一清理所有资源

清理顺序（逆序）：
  回调函数 → 可清理对象 → 文件 → CARLA actors → 恢复异步模式
```

---

## 🖥️ 第3步：显示系统

### **display_manager.py** - Pygame 显示管理器
**职责**：管理 Pygame 窗口、HUD 界面、摄像头显示

```python
# display_manager.py - 482行
核心组件：
1. CarlaCameraManager：管理 RGB 摄像头
   - 3种视角：后上方、车内、后方
   - 按 V 键切换视角

2. DisplayManager：主显示类
   - init_camera_manager()：初始化摄像头
   - handle_display_events()：处理 H/I/V 键
   - render_display()：渲染 HUD 界面

HUD界面元素：
   - 速度表盘（左侧圆形仪表）
   - ACC状态盘（右侧扇形仪表）
   - 状态指示条（ACC SYS, CONTROL, TORQUE）
   - 参数信息表（V_target, G2, 距离等）
   - 控制条（油门/刹车/方向盘）

关键属性：
   - self.show_info = False  # 默认关闭HUD，按I键打开
   - self.show_help = False  # 按H键显示帮助
```

---

## 🚗 第4步：CARLA 系统初始化

### **carla_system_initializer.py** - CARLA 世界初始化器

```python
# carla_system_initializer.py - 477行

输入：ACCConfig 配置对象
输出：CarlaSystemResources 数据类（封装所有CARLA资源）

初始化流程：
1. 连接 CARLA 服务器 (localhost:2000)
2. 加载地图 (acc_30km_new)
3. 设置同步模式 (fixed_delta_seconds=0.05s, 20FPS)
4. 生成车辆：
   - ego_vehicle: Audi e-tron (自主控制)
   - target_vehicle: Tesla Model3 (Traffic Manager)
5. 配置 Traffic Manager：
   - 设置前车目标速度
   - 禁用自动换道（场景模式需要）
   - 配置跟车距离
6. 初始化传感器和控制器：
   - CarlaPerception（感知模块）
   - TorqueToThrottleConverter（扭矩转换）
   - RampSpeedController（斜坡速度）
   - LaneChangeController（换道控制）
7. 初始化场景管理器（可选）：
   - CutInScenarioManager（切入场景）
   - CutOutScenarioManager（切出场景）
8. 设置交通灯为绿灯并冻结

返回：CarlaSystemResources（包含上述所有资源）
```

**相关文件**：

### **carla_perception.py** - 感知模块
```python
# 功能：
- get_vehicle_distance()：计算前车距离
- get_lane_offset()：计算车道偏移
- get_lookahead_offset()：计算前瞻偏移（用于横向控制）
```

### **vehicle_utils.py** - 车辆工具函数
```python
# 静态方法：
- get_vehicle_speed(vehicle)：获取车速（km/h 和 m/s）
- get_vehicle_distance(ego, target)：计算两车距离
- create_vehicle_control()：创建 VehicleControl 对象
```

---

## 🎮 第5步：控制系统

### **acc_control_facade.py** - ACC 控制外观模式

```python
# acc_control_facade.py - 105行
设计模式：外观模式（Facade Pattern）

职责：
  统一封装 ACC 决策+控制功能，简化上层调用

核心组件：
  self.manager = IntegratedSimulinkManager（实际干活的）

主要方法：
1. run_control(ego_speed_ms, command_type, control_error, control_mode_flag)
   → 调用 Simulink 执行 Decision + SPPVT
   → 返回：unified_output (包含决策和控制输出)

2. get_state() → 获取当前 ACC 状态
3. update_parameters(params) → 更新 ACC 参数（V_target, G2等）
4. cleanup() → 清理资源

状态追踪：
  - torque_arbitration_active：是否处于扭矩仲裁模式
  - call_count：调用次数
  - total_processing_time：总处理时间
```

### **integrated_simulink_manager.py** - Python-Simulink 桥接器（核心！）

```python
# integrated_simulink_manager.py - 592行（重要！）

核心职责：
  Python 和 Simulink 的桥接器，管理所有状态

设计理念：
  - Python维护状态（Decision状态、SPPVT状态）
  - Simulink只做纯函数计算（无状态）
  - 避免因果环

UDP通信协议：
  输入 (9个double):
    [0-3] Decision输入:
      - current_state (S0/S1/S2/S3)
      - command_type (I0-I6 键盘指令)
      - has_history (是否有历史)
      - last_active_decision (R1-R8)

    [4-8] SPPVT输入:
      - error_value (控制误差，已过滤)
      - stage_offset (阶段偏移累积)
      - prev_error, prev_velocity, prev_accel

  输出 (10个double):
    [0-4] Decision输出:
      - next_state
      - decision (R1-R8)
      - control_enabled (是否启用控制)
      - next_has_history
      - next_last_decision

    [5-9] SPPVT输出:
      - control_output (控制输出，扭矩/加速度)
      - velocity_output, acceleration_output, jerk_output
      - should_upgrade (是否应升级stage)

主要方法：
1. run_control() → 调用 Simulink 一次
2. _update_decision_state() → 更新 Decision 状态
3. _update_sppvt_state() → 更新 SPPVT 状态（关键！）
   - 检测误差符号翻转 → 重置 stage
   - 检测 G2 参数突变 → 重置
   - 检测误差突变 → 重置
   - 升级冷却管理

状态管理（Python端）：
  decision_state: {current_state, has_history, last_active_decision}
  sppvt_state: {stage_offset, stage, error_sign, prev_error, ...}
  params: {V_target_kmh, G2_s, V_min_kmh}
```

### **simulink_udp_interface.py** - UDP 通信客户端

```python
# 功能：
- 封装 socket UDP 通信
- call(inputs) → 发送数据到 Simulink，接收输出
- 双调用策略：预热调用+实际调用（解决延迟问题）
```

---

## ⌨️ 第6步：输入系统

### **input_manager.py** - 键盘输入管理器

```python
# input_manager.py - 246行

职责：
1. 处理键盘按下/释放事件
2. 管理按键去抖动（Space 键 0.5s）
3. 计算手动输入值（油门/刹车/转向）
4. 生成键盘指令码 (I0-I6)

按键映射：
  - ESC: 退出
  - Space: ACC 开关（去抖动）
  - Q/E: 调速 (±5 km/h)
  - R/T: 调距 (±0.2 s)
  - W/S: 油门/刹车（按住持续生效）
  - A/D: 转向（按住持续生效）
  - C: 取消 ACC
  - F: 触发斜坡速度
  - Z/X: 前车换道
  - P: 调试模式

指令码定义：
  I0: E降速, I1: Q加速
  I2: T减距, I3: R加距
  I4: W油门, I5: S刹车
  I6: C取消

关键方法：
1. process_keydown_event(key, acc_enabled) → 返回事件字典
2. process_keyup_event(key) → 处理按键释放
3. update_manual_inputs() → 更新手动输入状态（每帧调用）
   - 累加油门/刹车（按住时）
   - 更新转向角度
   - 返回 ManualInputState 对象
```

### **system_state.py** - 状态对象封装

```python
# 数据类定义：
1. ManualInputState：手动输入状态（油门/刹车/转向/W键/S键）
2. KeyboardCommand：键盘指令（code, description）
3. EgoState：自车状态（速度/油门/刹车/转向）
4. TargetState：目标车状态（速度）
5. ACCState：ACC 状态（开关/参数）
6. SystemState：系统总状态（聚合上述所有状态 + frame_count）

优势：
  - 避免全局变量
  - 类型安全
  - 便于调试和序列化
```

---

## 🔄 第7步：控制循环核心

### **control_loop_manager.py** - 控制循环管理器（核心！）

```python
# control_loop_manager.py - 543行（核心！）

职责：
  执行单个控制周期的完整流程（从感知到控制）

核心方法：
  run_single_step(manual_input_state) → StepResult

单步执行流程：

1. 更新仿真时间
   self.sim_elapsed_s += 0.05

2. 更新场景（如果有）
   - 检测切入/切出触发时间
   - 执行场景动作

3. 更新前车速度控制
   - 斜坡速度控制（F键触发）
   - 换道控制（Z/X键）

4. 感知环境
   - 获取自车速度、前车速度
   - 计算前车距离、车道偏移
   - calculate_two_mode_control_error()
     → 根据车速选择 TIME/SPEED 模式
     → 计算控制误差

5. ACC 决策和控制
   - 调用 acc_controller.run_control()
   - 传入：ego_speed, command, error, mode_flag
   - 返回：unified_output (Decision + SPPVT)

6. 计算最终控制输出
   - 横向控制：lateral_controller.update()
   - 纵向控制：
     a. SPPVT输出 → 扭矩/减速度
     b. 扭矩转换器 → 油门/刹车
     c. 扭矩仲裁（驾驶员介入优先）

7. 应用车辆控制
   ego_vehicle.apply_control(VehicleControl(...))

8. 返回 StepResult（包含所有状态和输出）

其他方法：
- set_acc_system_enabled(enabled)：开关 ACC
- trigger_ramp_speed()：触发斜坡测试
- trigger_lane_change_left/right()：触发换道
- get_acc_params()：获取当前 ACC 参数
```

### **two_mode_controller.py** - 双模式控制器

```python
# two_mode_controller.py - 429行

核心功能：
  根据车速自动切换控制模式

两种模式：
1. TIME模式（时距控制）：
   - 触发条件：ego_speed ≤ V_threshold (默认50 km/h)
   - 控制目标：期望时距 G2 (默认4.0s)
   - 期望距离：D_desired = G2 × ego_speed + D0
   - 控制误差：error = G2 - (distance / ego_speed)
   - 误差符号：负值→太近，正值→太远

2. SPEED模式（速度控制）：
   - 触发条件：ego_speed > V_threshold
   - 控制目标：目标速度 V_target
   - 控制误差：error = V_target - ego_speed
   - 误差符号：负值→太快，正值→太慢

关键函数：
1. set_two_mode_parameters(V_threshold, G2, target_speed)
   → 设置全局参数

2. calculate_two_mode_desired_distance(ego_speed_ms)
   → 返回：(desired_distance, control_mode)

3. enhanced_two_mode_control(ego_speed, distance, target_speed)
   → 返回：{
       'control_error': float,
       'control_mode_flag': int,  # 1=TIME, 0=SPEED
       'reference_value': float,   # 参考值（G2或V_target）
       'current_value': float      # 当前值（actual_gap或ego_speed）
     }
```

### **enhanced_lateral_controller.py** - 横向控制器

```python
# 功能：
- 预瞄PID控制
- 组合误差：α × current_offset + β × lookahead_offset
- 动态预瞄距离：base + gain × speed (5m-15m)
- PID计算 → 转向角度（-1 到 1）
```

### **torque_to_throttle_converter.py** - 扭矩转换器

```python
# torque_to_throttle_converter.py - 297行

功能：
  物理模型转换（SPPVT输出 → CARLA控制值）

硬编码参数（基于 CARLA Audi e-tron）：
  VEHICLE_MASS = 2370 kg
  WHEEL_RADIUS = 0.37 m
  MAX_BRAKE_TORQUE = 1000 N·m per wheel
  THEORETICAL_MAX_DECEL = 4.56 m/s²

转换流程：
1. 加速模式：扭矩 → 油门
   - 计算发动机 RPM = f(speed, gear_ratio)
   - 从扭矩曲线插值获取 max_torque(rpm)
   - throttle = desired_torque / max_torque

2. 减速模式：减速度 → 刹车
   - F_total = mass × |decel|
   - T_per_wheel = (F / 4) × radius
   - brake = T_per_wheel / MAX_BRAKE_TORQUE

关键方法：
  engine_torque_to_throttle(torque, speed_kmh)
    → 返回：(throttle, brake)
```

---

## 📊 第8-9步：数据记录与可视化

### **CSV 记录**（在 acc_updated.py 中）

```python
# 字段：
Time, Ego_Speed, Target_Speed, Actual_Distance, Desired_Distance,
Control_Mode, Lane_Offset, ACC_State, ACC_Active,
V_target_Setting, V_min_Setting, G2_Setting,
Manual_Throttle, Manual_Brake, Manual_Steer

# 优化：每秒 flush 一次（而非每帧）
```

### **result_plotter.py** - 结果保存绘图器

```python
# 608行 - RealtimeResultPlotter
模式：Step模式（按帧数）
功能：
  - 实时绘制 8 个子图
  - 程序结束时自动保存图像
  - 使用独立线程更新（避免阻塞主循环）

子图：
  1. 期望时距 vs 实际时距
  2. 自车速度 vs 前车速度
  3. ACC控制状态
  4. 请求扭矩
  5. 请求减速度
  6-8. （其他指标）
```

### **realtime_time_gap_plotter.py** - 实时测试绘图器

```python
# RealtimeTimeGapPlotter
模式：Time模式（按时间）
功能：
  - 实时显示时距跟踪
  - 适合调试和实时观察
```

### **output_formatter.py** - 输出格式化工具

```python
# 功能：
1. format_system_info()：格式化 HUD 显示信息
2. print_simulink_io()：打印 Simulink I/O 调试信息
3. 统一输出格式，避免重复代码
```

---

## 🎬 场景控制文件

### **cut_in_scenario.py** - 切入场景管理器

```python
# 212行
流程：
1. 在侧向车道生成切入车辆
2. 等待触发时间（默认25s）
3. 强制变道到自车前方
4. 切换跟车目标：target_vehicle ← cut_in_vehicle
```

### **cut_out_scenario.py** - 切出场景管理器

```python
# 流程：
1. 生成 2 个前车（lead + cut_vehicle）
2. 初始跟车：cut_vehicle
3. 触发时刻：cut_vehicle 变道离开
4. 切换跟车：target_vehicle ← lead_vehicle
```

### **ramp_speed_controller.py** - 斜坡速度控制器

```python
# 功能：
- 前车速度线性变化测试
- 按 F 键触发
- 参数：start_speed → target_speed (持续 10s)
```

### **lane_change_controller.py** - 换道控制器

```python
# 功能：
- 控制前车向左/右换道
- 按 Z/X 键触发
- 使用 Traffic Manager 强制换道
```

---

## 📂 辅助工具文件

### **matlab_engine_factory.py** - MATLAB 引擎工厂（已废弃）

```python
# 旧架构使用，现在已不需要
# 使用 UDP 通信替代 MATLAB Engine API
```

### **acc_controller.py** - ACC 状态枚举

```python
# 定义：
class ACCState(Enum):
    STANDBY = 0
    ACTIVE = 1
    FAULT = 2
```

---

## 🎯 数据流总结

```
用户按键 (InputManager)
    ↓
控制循环开始 (ControlLoopManager.run_single_step)
    ↓
1. 场景更新 (CutInManager/CutOutManager)
2. 前车速度控制 (RampController/LaneChangeController)
    ↓
3. 感知环境 (CarlaPerception)
   - 前车距离、车道偏移
    ↓
4. 计算控制误差 (TwoModeController)
   - TIME模式: error = G2 - (distance/speed)
   - SPEED模式: error = V_target - ego_speed
    ↓
5. ACC决策+控制 (ACCControlFacade → IntegratedSimulinkManager)
   - Python 准备输入 (9个double)
   - UDP 发送到 Simulink (27000)
   - Simulink 计算 (Decision + SPPVT)
   - UDP 接收输出 (27001, 10个double)
   - Python 更新状态
    ↓
6. 扭矩转换 (TorqueToThrottleConverter)
   - SPPVT输出 → 扭矩 → 油门/刹车
    ↓
7. 横向控制 (EnhancedLateralController)
   - 预瞄PID → 转向角
    ↓
8. 扭矩仲裁（如果驾驶员介入）
   - throttle = max(ACC, driver)
    ↓
9. 应用控制 (ego_vehicle.apply_control)
    ↓
10. 数据记录 (CSV + Plotter)
11. HUD显示 (DisplayManager)
```

---

## 🔑 关键设计模式总结

1. **外观模式**：`ACCControlFacade` 封装复杂的 ACC 逻辑
2. **数据类模式**：`SystemState`, `StepResult` 避免全局变量
3. **资源管理模式**：`ResourceManager` 确保清理
4. **状态分离模式**：Python 维护状态，Simulink 纯计算
5. **管理器模式**：各种 Manager 类分离职责
